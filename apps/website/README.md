# GitPier Docs

GitPier documentation site.

## Local Development

```bash
pnpm install
pnpm dev
```

## Deployment (Docker Compose)

```bash
git clone ssh://git@git.gitpier.com/gitpier/docs.git
cd docs
docker compose up -d --build
```
